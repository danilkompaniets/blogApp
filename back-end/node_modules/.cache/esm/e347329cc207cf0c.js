let express,dotenv;_c20‍.w("express",[["default",["express"],function(v){express=v}]]);_c20‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();
const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.send("server is running");
});

const PORT = process.env.PORT || 

app.listen(PORT, () => console.log(`server is running on port `))
